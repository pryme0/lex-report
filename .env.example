# Base URL of the LexReport API, including the /api prefix.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Optional. The API has no authentication yet; workspace endpoints fall back to a seeded
# demo user when this is unset. Set it to act as a specific user id.
NEXT_PUBLIC_API_USER_ID=
